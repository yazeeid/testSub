name: master

on:
  push:
    branch:
      - master
  schedule:
    - cron: '* * * * *' 

jobs:
  bulid:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
      - uses: actions/checkout@v1 
        run: |
          echo "Running"
